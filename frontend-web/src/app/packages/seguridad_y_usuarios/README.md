# Paquete `seguridad_y_usuarios` — Frontend Web (Angular)

Casos de uso (lista maestra de `Contexto.md`): **CU01, CU02, CU03, CU04, CU05, CU36** — Ciclo 1.
Componentes: `login/`, `register/`, `recover/`, `usuarios_roles/`, `audit/`.
Servicios/guardas: `auth.service`, `users.service`, `auth.guard` (AuthGuard/SessionGuard), `auth.interceptor`.
